[package]
name = "sigma-identity-nav"
version = "0.1.0"
edition = "2024"
rust-version = "1.90.0"
license = "MIT OR Apache-2.0"
authors = ["Sigma Tactical Group contributors <https://github.com/sigmatactical-org>"]
description = "Reusable Sigma identity sign-in / welcome navbar widget and URL builders"
publish = false

[dependencies]
askama = "0.16.0"
sigma-cart-nav = { git = "https://github.com/sigmatactical-org/cart.git" }
sigma-contact-nav = { git = "https://github.com/sigmatactical-org/contact.git" }

[package.metadata.askama]
dirs = ["templates"]
